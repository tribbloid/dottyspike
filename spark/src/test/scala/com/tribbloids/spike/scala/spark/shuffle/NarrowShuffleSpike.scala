package com.tribbloids.spike.scala.spark.shuffle

import ai.acyclic.six.spark.SparkEnvSpec

class NarrowShuffleSpike extends SparkEnvSpec {

  describe("can create an RDD that depends on") {

    it("a subset of another RDD's partitions") {}

    it("... only the partitions that finished execution") {}
  }

  it("")
}
